.env
auto-hack.js
botnames.txt
passwords.txt
node_modules
.command_history
bannana
node_modules/
.env*
data/*.json
# Append-only measurement history (coinflips, time series) — generated, not source.
data/*.jsonl
data/*.jsonl.*
cron-jobs.json
removed-bots.json
# Local web-client build (dist + cloned upstream src + build.log).
# Built on demand by `npm run web-client:build` or automatically on first /play.
web-client/
